services:
  palantir:
    build: .
    container_name: palantir
    restart: always
    environment:
      CHAT_ID: ${CHAT_ID}
      TELEGRAM_API_KEY: ${TELEGRAM_API_KEY}
      TICKERS: AAPL,AMZN,GOOGL,META,MSFT,NVDA,TSLA,AVGO,KO,MCD,NKE,ORCL,PLTR,QQQ,TSM,VOO,WMT,AMD,CMG,CVX,DEO,INTC,MA,MNST,PEP,PFE,PYPL,SHEL,SHOP,V,YPF,BRK-B
      FETCH_EVERY: 300
